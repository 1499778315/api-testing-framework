# 统一换行符，避免 Windows / Linux 之间协作时出现整文件 diff。
# Shell 脚本必须保持 LF，否则在 Linux / CI 上会报
# "bash: ./run_demo.sh: /bin/bash^M: bad interpreter"。
* text=auto
*.sh  text eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
